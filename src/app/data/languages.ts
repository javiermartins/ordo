import { Language } from "../models/language.model";

const languages: Language[] = [
    { id: 'en', value: 'LANGUAGES.ENGLISH' },
    { id: 'es', value: 'LANGUAGES.SPANISH' }
];

export default languages;